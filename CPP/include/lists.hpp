#ifndef LISTS
#define LISTS

#include <vector>

std::vector<No> Nos;          // Errado pois eh classe
std::vector<Pacotes> Pacotes; // Errado pois eh classe
std::vector<int> indicesParaEnvivo;
std::vector<int> indicesParaVer;
std::vector<int> proximoAEnviar;

#endif // !LISTS
